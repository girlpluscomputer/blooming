query GetHabits {
  habits {
    id
    title
    description
    category
    completed
    currentDay
    totalOfDays
    createdAt
    logs {
      id
      createdAt
      expiresAt
      completed
      status
    }
    user {
      id
      email
    }
  }
}
